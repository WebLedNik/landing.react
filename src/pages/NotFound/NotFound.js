import React from "react";
import './NotFound.css'

function NotFound() {
    return (
        <main className={'container not-found'}>
            NotFound
        </main>
    )
}

export default NotFound